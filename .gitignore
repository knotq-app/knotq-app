.DS_Store
target
data
build
.svelte-kit
node_modules/

/local/.env
/local/*.env

.claude/CLAUDE.local.md
.claude/settings.local.json

# Self-contained local run artifacts.
.knotq-run/

# Mobile lives in its own nested repository.
mobile/

# Backend lives in its own nested repository.
backend/

# Local release/build scratch (xcarchives, exported IPAs, dSYMs). Never commit.
.tmp/

# Release build outputs and signing material, wherever they land. These were
# committed once (under .tmp/) and had to be rewritten out of main.
*.xcarchive/
*.ipa
*.dSYM/
*.mobileprovision
*.p12
*.p8
GoogleService-Info.plist
*.aab
*.apk
